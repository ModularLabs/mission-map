import { Cell } from "./gridData";

describe("gridData factory functions", () => {
  describe("Cell", () => {
    it("checks arguments", () => {
      // expect(() => {
      //   Cell("octopus");
      // }).toThrow();
      expect(1).toBe(1);
    });
  });
});
